name: Automatic Documentation Translation

on:
  push:
    branches:
      - main
    paths:
      - 'README.md'
      - 'CHANGELOG.md'
      - 'docs/**'

jobs:
  translate:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Argos Translate
        run: |
          python -m pip install --upgrade pip
          pip install argostranslate

      - name: Run translation script
        run: python scripts/translate_docs_argos.py

      - name: Commit translated docs
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add docs/
          git commit -m "Automatic translation update" || echo "No changes to commit"
          git push origin main
